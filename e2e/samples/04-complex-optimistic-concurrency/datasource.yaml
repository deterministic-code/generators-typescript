version: 1.0.0
includes:
  - types:
      filter: tag == "datasource_type"
types:
  - status:
      fields:
        - name:
            is_unique: true
  - project:
      use_optimistic_concurrency: true
      fields:
        - name:
            is_unique: true
  - task:
      use_optimistic_concurrency: true
