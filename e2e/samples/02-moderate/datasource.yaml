version: 1.0.0
includes:
  - types:
      filter: tag == "datasource_type"
types:
  - role:
      fields:
        - name:
            is_unique: true
  - user:
      fields:
        - email:
            is_unique: true
