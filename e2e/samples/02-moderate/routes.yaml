version: 1.0.0
includes:
  - types:
      filter: tag == "view_type"
routes:
  - users_by_email:
  - ping:
      method: POST
      path: /api/ping
      request: missing_shape
      response: missing_shape
  - clone_user:
      method: POST
      path: /api/users/:id/clone/:id
